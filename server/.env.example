# NarrativeRebuild real-time backend configuration
# Copy to `.env` to override defaults. All values are optional.

# HTTP + Socket.io port for the Node backend.
# Default: 4000
# PORT=4000

# Port of the Python NLP microservice (pronoun analyzer).
# Default: 5000
# NLP_PORT=5000
